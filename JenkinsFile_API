pipeline {
    agent any   
    stages {
        stage('Run API TESTS') {
            steps {
                // Execute Python script
                bat "python API_Test_runner.py"
            }
        }
    }
    
    post {
        always {
            cleanWs()
        }
    }
}
